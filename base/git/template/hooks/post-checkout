#!/bin/sh

hook="${XDG_CONFIG_HOME:-$HOME/.config}/git/hooks/setup-cloned-repo"

if [ ! -x "$hook" ]; then
	echo "post-checkout: clone setup is not installed at $hook" >&2
	exit 1
fi

exec "$hook" "$@"
