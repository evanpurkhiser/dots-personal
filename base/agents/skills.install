#!/usr/bin/env bash

source "$DOTS_SOURCE/base/bash/environment"

for tool in opencode claude codex; do
	config_home="$XDG_CONFIG_HOME/$tool"
	target="$config_home/skills"

	mkdir -p "$config_home"
	rm -rf "$target"
	ln -sfn "$XDG_CONFIG_HOME/agents/skills" "$target"
done
