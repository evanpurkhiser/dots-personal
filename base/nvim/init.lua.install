#!/usr/bin/env bash

source "$DOTS_SOURCE/base/bash/environment"

# Install vim dependnecies
if hash nvim 2>/dev/null; then
	nvim --headless -c 'autocmd User PackerComplete quitall' -c 'PackerSync'
fi
