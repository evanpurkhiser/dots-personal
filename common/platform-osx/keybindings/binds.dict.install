#!/usr/bin/env bash

source "$DOTS_SOURCE/base/bash/environment"

mkdir -p "$HOME/Library/KeyBindings/"
ln -sf "$XDG_CONFIG_HOME/keybindings/binds.dict" \
       "$HOME/Library/KeyBindings/DefaultKeyBinding.dict"
