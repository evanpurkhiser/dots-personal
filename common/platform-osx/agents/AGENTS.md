The SSH agent (used for git signing, ssh, ansible, etc.) is backed by 1Password. If a command fails with a signing error like `sign_and_send_pubkey: signing failed for ED25519 ... communication with agent failed`, or any operation that needs SSH/git auth fails unexpectedly, it's almost always because I'm AFK or did not approve the 1Password prompt. Pause and ask me to authenticate, then retry — do not start debugging the SSH config.
